# Provider selection: "agent", "glm", or "openai_compatible"
# Use "agent" when Hermes Agent should answer prompts with its currently selected model.
AUTONOVEL_PROVIDER=agent

# Default language for book-facing content. Technical JSON keys stay English.
AUTONOVEL_LANGUAGE=ru

# Hermes Agent request/response file bridge.
AUTONOVEL_AGENT_REQUEST_DIR=.autonovel/agent_requests
# AUTONOVEL_AGENT_RESPONSE_FILE=

# Z.AI/GLM API key (required only when using glm provider)
GLM_API_KEY=your-glm-api-key-here
GLM_BASE_URL=https://api.z.ai/api/coding/paas/v4

# Generic OpenAI-compatible fallback provider
MODEL_API_KEY=your-model-api-key-here
MODEL_API_BASE_URL=https://api.example.invalid/v1
MODEL_API_HEADERS={}

# Writer model (used for drafting and revision)
AUTONOVEL_WRITER_MODEL=auto

# Judge model (used for evaluation — should differ from writer)
AUTONOVEL_JUDGE_MODEL=auto

# Review model (deep prose-level analysis, used in Phase 3b)
AUTONOVEL_REVIEW_MODEL=auto

# Optional metadata for reusable export/art scripts
AUTONOVEL_TITLE=Untitled Novel
AUTONOVEL_AUTHOR=Author

# fal.ai key for image generation (Nano Banana 2)
FAL_KEY=your-fal-api-key-here

# ElevenLabs API key for audiobook generation
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here
